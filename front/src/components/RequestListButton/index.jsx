import React from "react";

const RequestListButton = props => {
    return <button onClick={ props.requestList } className="waves-effect waves-light btn btn-size">Zgłoszenia oczeukjące</button>
}

export default RequestListButton